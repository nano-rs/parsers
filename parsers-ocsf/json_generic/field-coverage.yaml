source: json
transforms:
- source: '@timestamp'
  kind: epoch_ms
  target: time
  format: '%+'
  reason: Existing timestamp conversion verified against the emitted millisecond clock.
- source: level
  kind: lowercase
  reason: Case normalization already performed by the parser; observed at metadata.log_level,
    and checked against the emitted branch.
- source: timestamp
  kind: epoch_ms
  target: time
  format: '%+'
  reason: Existing timestamp conversion verified against the emitted millisecond clock.
- source: protocol
  kind: lowercase
  reason: Case normalization already performed by the parser; observed at connection_info.protocol_name,
    and checked against the emitted branch.
- source: time
  kind: scale
  factor: 1000
  target: time
  reason: Existing unit conversion by 1000 verified against time; unused clock fallbacks
    remain residual.
- source: datetime
  kind: epoch_ms
  target: time
  format: '%Y-%m-%d %H:%M:%S'
  reason: Existing timestamp conversion verified against the emitted millisecond clock.
consumed_candidates:
- cmdline
- command_line
- dest_port
- destination_port
- domain
- dst_port
- event_code
- file_name
- file_path
- hash
- image
- message
- msg
- pid
- process_id
- process_name
- protocol
- referer
- referrer
- sha256
- source_port
- src_port
- user_agent
consumed_reason: Only source keys read by this parser are eligible for exact-value
  consumption; the selected branch must emit the value. Unknown keys always spill,
  even when their value equals an OCSF default.
