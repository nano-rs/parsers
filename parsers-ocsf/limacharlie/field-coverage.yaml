source: json
transforms:
- source: routing.hostname
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: routing.int_ip
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.USER_NAME
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.HASH
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.PARENT.HASH
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.ACCESS_MASK
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.DOMAIN_NAME
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.IP_ADDRESS
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.DOMAIN
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.SOURCE_IP
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.HOSTNAME
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.TYPE
  kind: enum
  target: query.type
  values:
    aaaa: AAAA
  reason: Parser normalizes the caption case; verify the emitted caption.
- source: event.PARENT.USER_NAME
  kind: lowercase
  reason: Case normalization already performed by the parser; observed at actor.user.name,
    and checked against the emitted branch.
- source: event.ACCESS_MASK
  kind: hex
  target: actual_permissions
  reason: Parsed hexadecimal process ID or access mask is represented as an integer
    at this destination.
- source: event.SOURCE.USER_NAME
  kind: lowercase
  reason: Case normalization already performed by the parser; observed at actor.user.name,
    and checked against the emitted branch.
consumed_candidates:
- event
- event.ACCESS_MASK
- event.BASE_ADDRESS
- event.COMMAND_LINE
- event.DESTINATION
- event.DESTINATION.IP
- event.DESTINATION.PORT
- event.DNS_TYPE
- event.DOMAIN
- event.DOMAIN_NAME
- event.ERROR
- event.FILE_PATH
- event.FILE_SIZE
- event.HANDLES
- event.HASH
- event.HOSTNAME
- event.IP_ADDRESS
- event.IP_ADDRESSES
- event.LOGON_TYPE
- event.MEMORY_SIZE
- event.NAME
- event.NETWORK_ACTIVITY
- event.NEW_VALUE
- event.OLD_VALUE
- event.PARENT
- event.PARENT.COMMAND_LINE
- event.PARENT.FILE_PATH
- event.PARENT.HASH
- event.PARENT.PROCESS_ID
- event.PARENT_PROCESS_ID
- event.PATH
- event.PROCESS_ID
- event.REGION
- event.REGISTRY_PATH
- event.RULE_NAME
- event.RULE_NS
- event.SERVICE
- event.SESSION_ID
- event.SEVERITY
- event.SIGNATURE_STATUS
- event.SIGNATURE_VALID
- event.SIGNERS
- event.SOURCE
- event.SOURCE.FILE_PATH
- event.SOURCE.IP
- event.SOURCE.PORT
- event.SOURCE.PROCESS_ID
- event.SOURCE.USER_NAME
- event.SOURCE_IP
- event.START_ADDRESS
- event.SUMMARY
- event.SVC_BINARY
- event.SVC_DISPLAY_NAME
- event.SVC_NAME
- event.SVC_START_TYPE
- event.SVC_STATE
- event.TARGET
- event.TARGET.FILE_PATH
- event.TARGET.PROCESS_ID
- event.TARGET.USER_NAME
- event.TYPE
- event.USER_ID
- event.USER_NAME
- event.VALUE
- event.VALUE_NAME
- event.author
- event.cat
- event.detect_id
- event.source
- event.summary
- routing
- routing.event_id
- routing.event_time
- routing.event_type
- routing.hostname
- routing.int_ip
- routing.parent
- routing.sid
- routing.this
consumed_reason: Only source keys read by this parser are eligible for exact-value
  consumption; the selected branch must emit the value. Unknown keys always spill,
  even when their value equals an OCSF default.
