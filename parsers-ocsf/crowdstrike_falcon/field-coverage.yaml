source: json
transforms:
- source: ComputerName
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.ComputerName
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: Hostname
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.Hostname
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: aip
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.aip
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: UserName
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.UserName
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: UserId
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.UserId
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: LogonDomain
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.LogonDomain
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: MachineDomain
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.MachineDomain
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: SHA256HashData
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.SHA256HashData
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: SHA256String
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.SHA256String
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: SHA1HashData
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.SHA1HashData
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: MD5HashData
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.MD5HashData
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: MD5String
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.MD5String
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: LocalAddressIP4
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.LocalAddressIP4
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: LocalAddressIP6
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.LocalAddressIP6
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: RemoteAddressIP4
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.RemoteAddressIP4
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: RemoteAddressIP6
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.RemoteAddressIP6
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: DomainName
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.DomainName
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: AuthenticationPackage
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.AuthenticationPackage
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: UserIp
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.UserIp
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: Success
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.Success
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: LocalIP
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.LocalIP
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: PatternDispositionDescription
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.PatternDispositionDescription
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: State
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.State
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event_platform
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: event.event_platform
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: '@timestamp'
  kind: epoch_ms
  target: time
  reason: ISO clock converted to epoch milliseconds; only the selected successful
    clock is consumed.
- source: event.@timestamp
  kind: epoch_ms
  target: time
  reason: ISO clock converted to epoch milliseconds; only the selected successful
    clock is consumed.
- source: ContextTimeStamp
  kind: scale
  factor: 1000
  target: time
  reason: Falcon epoch seconds converted to milliseconds; verify the emitted event
    clock.
- source: ProcessStartTime
  kind: scale
  factor: 1000
  target: time
  reason: Falcon epoch seconds converted to milliseconds; verify the emitted event
    clock.
- source: event.ContextTimeStamp
  kind: scale
  factor: 1000
  target: time
  reason: Falcon epoch seconds converted to milliseconds; verify the emitted event
    clock.
- source: event.ProcessStartTime
  kind: scale
  factor: 1000
  target: time
  reason: Falcon epoch seconds converted to milliseconds; verify the emitted event
    clock.
- source: TaskAuthor
  kind: lowercase
  reason: Case normalization already performed by the parser; observed at job.user.name,
    and checked against the emitted branch.
consumed_candidates:
- AgentIdString
- AppInstallPath
- AppInstalledDate
- AppName
- AppVendor
- AppVersion
- AsepClass
- AuthenticationId
- AuthenticationPackage
- BuildNumber
- BytesReceived
- BytesSent
- CommandLine
- CompositeId
- ComputerName
- ConnectionDirection
- ConnectionFlags
- ContentType
- ContextProcessId
- ContextTimeStamp
- DetectDescription
- DetectId
- DetectName
- DnsRequestCount
- DomainName
- EventOrigin
- EventUUID
- FalconHostLink
- FileIdentifier
- FileName
- FileOperatorSid
- FilePath
- FineScore
- Hostname
- IOCType
- IOCValue
- IP4Records
- IP6Records
- ImageFileName
- InContext
- IncidentId
- IncidentType
- InjectedProcessId
- InjectedProcessImageFileName
- InjectedThread
- InjectingProcessId
- InjectingProcessImageFileName
- IntegrityLevel
- IsOnNetwork
- IsOnRemovableDisk
- LateralMovement
- LocalAddressIP4
- LocalAddressIP6
- LocalIP
- LocalPort
- LogonDomain
- LogonId
- LogonServer
- LogonType
- MD5HashData
- MD5String
- MachineDomain
- MajorVersion
- MinorVersion
- OperationName
- ParentBaseFileName
- ParentCommandLine
- ParentImageFileName
- ParentProcessId
- PatternDispositionDescription
- PatternDispositionValue
- ProcessGroupId
- ProcessId
- ProcessStartTime
- ProductName
- ProductType
- Protocol
- QueryStatus
- RawProcessId
- RemoteAccount
- RemoteAddressIP4
- RemoteAddressIP6
- RemotePort
- RequestType
- SHA1HashData
- SHA256HashData
- SHA256String
- ScriptContent
- ScriptContentName
- ScriptContentSource
- ServiceDisplayName
- ServiceImagePath
- ServiceName
- ServiceObjectName
- ServicePackMajor
- ServiceStartType
- ServiceType
- Severity
- SeverityName
- SignInfoFlags
- Size
- SourceProcessId
- State
- Status
- SubStatus
- Success
- Tactic
- TargetDirectoryName
- TargetFileName
- TargetObjectData
- TargetObjectName
- TargetObjectPath
- TargetProcessId
- TaskAuthor
- TaskExecArguments
- TaskExecCommand
- TaskName
- Technique
- TokenType
- UTCTimestamp
- UserId
- UserIp
- UserIsAdmin
- UserName
- UserSid
- aid
- aip
- event
- event.AgentIdString
- event.AppInstallPath
- event.AppInstalledDate
- event.AppName
- event.AppVendor
- event.AppVersion
- event.AsepClass
- event.AuthenticationId
- event.AuthenticationPackage
- event.BuildNumber
- event.BytesReceived
- event.BytesSent
- event.CommandLine
- event.CompositeId
- event.ComputerName
- event.ConnectionDirection
- event.ConnectionFlags
- event.ContentType
- event.ContextProcessId
- event.ContextTimeStamp
- event.DetectDescription
- event.DetectId
- event.DetectName
- event.DnsRequestCount
- event.DomainName
- event.EventOrigin
- event.EventUUID
- event.FalconHostLink
- event.FileIdentifier
- event.FileName
- event.FileOperatorSid
- event.FilePath
- event.FineScore
- event.Hostname
- event.IOCType
- event.IOCValue
- event.IP4Records
- event.IP6Records
- event.ImageFileName
- event.InContext
- event.IncidentId
- event.IncidentType
- event.InjectedProcessId
- event.InjectedProcessImageFileName
- event.InjectedThread
- event.InjectingProcessId
- event.InjectingProcessImageFileName
- event.IntegrityLevel
- event.IsOnNetwork
- event.IsOnRemovableDisk
- event.LateralMovement
- event.LocalAddressIP4
- event.LocalAddressIP6
- event.LocalIP
- event.LocalPort
- event.LogonDomain
- event.LogonId
- event.LogonServer
- event.LogonType
- event.MD5HashData
- event.MD5String
- event.MachineDomain
- event.MajorVersion
- event.MinorVersion
- event.OperationName
- event.ParentBaseFileName
- event.ParentCommandLine
- event.ParentImageFileName
- event.ParentProcessId
- event.PatternDispositionDescription
- event.PatternDispositionValue
- event.ProcessGroupId
- event.ProcessId
- event.ProcessStartTime
- event.ProductName
- event.ProductType
- event.Protocol
- event.QueryStatus
- event.RawProcessId
- event.RemoteAccount
- event.RemoteAddressIP4
- event.RemoteAddressIP6
- event.RemotePort
- event.RequestType
- event.SHA1HashData
- event.SHA256HashData
- event.SHA256String
- event.ScriptContent
- event.ScriptContentName
- event.ScriptContentSource
- event.ServiceDisplayName
- event.ServiceImagePath
- event.ServiceName
- event.ServiceObjectName
- event.ServicePackMajor
- event.ServiceStartType
- event.ServiceType
- event.Severity
- event.SeverityName
- event.SignInfoFlags
- event.Size
- event.SourceProcessId
- event.State
- event.Status
- event.SubStatus
- event.Success
- event.Tactic
- event.TargetDirectoryName
- event.TargetFileName
- event.TargetObjectData
- event.TargetObjectName
- event.TargetObjectPath
- event.TargetProcessId
- event.TaskAuthor
- event.TaskExecArguments
- event.TaskExecCommand
- event.TaskName
- event.Technique
- event.TokenType
- event.UTCTimestamp
- event.UserId
- event.UserIp
- event.UserIsAdmin
- event.UserName
- event.UserSid
- event.aid
- event.aip
- event.event_platform
- event.event_simpleName
- event.timestamp
- event_platform
- event_simpleName
- metadata.eventCreationTime
- metadata.eventType
- metadata.offset
- timestamp
consumed_reason: Only source keys read by this parser are eligible for exact-value
  consumption; the selected branch must emit the value. Unknown keys always spill,
  even when their value equals an OCSF default.
