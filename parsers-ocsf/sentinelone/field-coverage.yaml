source: json
transforms:
- source: agentComputerName
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: agentName
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: data.computerName
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: agentIp
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: User
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: data.username
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: agentDomain
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: Sha256
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: Sha1
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: Md5
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: confidenceLevel
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: fileContentHash
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: mitigationStatus
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: data.ipAddress
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: EventType
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: SrcIP
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: DstIP
  kind: lowercase
  reason: Parser lowercases this hostname, address, identity or hash; consume only
    if the normalized value is emitted.
- source: createdAt
  kind: epoch_ms
  target: time
  reason: ISO clock converted to epoch milliseconds; only the selected successful
    clock is consumed.
- source: ProcessStartTime
  kind: epoch_ms
  target: time
  reason: ISO clock converted to epoch milliseconds; only the selected successful
    clock is consumed.
consumed_candidates:
- DnsRequest
- DstIP
- DstPort
- EventType
- FilePath
- Md5
- ParentProcessCommandLine
- ParentProcessId
- ParentProcessImagePath
- ParentProcessName
- ProcessCommandLine
- ProcessId
- ProcessImagePath
- ProcessName
- ProcessStartTime
- RegistryPath
- RegistryValue
- Sha1
- Sha256
- SrcIP
- SrcPort
- User
- accountId
- accountName
- activityType
- agentComputerName
- agentDomain
- agentId
- agentIp
- agentIsActive
- agentMachineType
- agentName
- agentNetworkStatus
- agentOsType
- agentVersion
- classification
- classificationSource
- cloudVerdict
- confidenceLevel
- createdAt
- data.computerName
- data.ipAddress
- data.role
- data.source
- data.username
- description
- engines
- fileContentHash
- filePath
- fileSize
- groupName
- id
- indicators
- initiatedBy
- mitigationStatus
- mitigationStatusDescription
- primaryDescription
- secondaryDescription
- siteName
- threatId
- threatName
- userId
consumed_reason: Only source keys read by this parser are eligible for exact-value
  consumption; the selected branch must emit the value. Unknown keys always spill,
  even when their value equals an OCSF default.
